// This page is intentionally empty — it exists so Next.js resolves
// the /account route. The layout handles conditional rendering of
// @dashboard (authenticated) or @login (guest) parallel slots.
export default function AccountPage() {
  return null
}
